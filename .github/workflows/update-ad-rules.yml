name: Update AdGuard Rules

on:
  schedule:
    - cron: '0 */12 * * *'  # 每12小时自动运行
  workflow_dispatch:  # 允许手动触发

jobs:
  update-rules:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: pip install pyyaml requests
        
      - name: Update rules file
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: Star7-Files-Hub
          REPO_NAME: Files
          RULES_PATH: Files/Ad/AdGuardHomeBlack.txt
        run: |
          python "${{ github.workspace }}/Files/py/update_ad_rules.py"
          
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ${{ env.RULES_PATH }}
          git commit -m "🔄 自动更新: 更新规则文件 [$(date '+%Y-%m-%d %H:%M:%S')]" || echo "No changes to commit"
          git push
